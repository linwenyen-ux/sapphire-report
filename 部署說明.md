# 裝機報告 PWA — 部署步驟說明

## 一、Google Apps Script 後端部署（Eric 需用自己的 Google 帳號操作）

1. 到 https://sheets.google.com 建立一個新的 Google 試算表，命名為「Applicator裝機報告」。
2. 在試算表選單點「擴充功能 → Apps Script」，會開啟一個新分頁的程式編輯器。
3. 把編輯器裡預設的 `function myFunction() {}` 全部刪除，貼上本資料夾內 `AppsScript_Code.gs` 的完整內容。
4. 確認 `AppsScript_Code.gs` 最上面的 `NOTIFY_EMAIL` 是否為你要接收通知信的信箱（目前設定為 `service@lightmed.com`，如需更改請直接改這一行）。
5. 點上方「執行」旁的函式選單，選 `test_doPost_installReport`，第一次執行會跳出「需要授權」，依畫面指示允許權限（會顯示「未經驗證的應用程式」警告，屬正常現象，點「進階」→「前往...（不安全）」繼續即可，因為這是你自己寫的腳本）。
6. 執行成功後回到 Google 試算表，應該會看到自動建立了「裝機報告」分頁並多了一列測試資料；同時 `service@lightmed.com` 信箱應收到一封測試通知信。確認無誤後，可以手動刪除這列測試資料。
6b. 再執行一次函式選單中的 `test_doPost_tipOrder`，確認會自動建立「耗材訂購」分頁並收到對應的訂單通知信（同樣測試完可刪除測試列）。
7. 回到 Apps Script 編輯器，點右上角「部署 → 新增部署作業」。
   - 類型選「網頁應用程式」。
   - 「執行身份」選「我」。
   - 「誰可以存取」選「所有人」（因為診所端填表不需要登入 Google 帳號）。
   - 點「部署」，會再次要求授權，同意即可。
8. 部署完成後會出現一個網址，格式類似：
   `https://script.google.com/macros/s/AKfycb.../exec`
   **這個網址就是要貼到前端表單的 Web App URL**，請複製起來。

## 二、把 Web App URL 貼進前端表單

1. 打開 `index.html`，搜尋這一行：
   ```
   const WEBAPP_URL = 'PASTE_YOUR_GOOGLE_APPS_SCRIPT_WEB_APP_URL_HERE';
   ```
2. 把單引號中間換成上一步拿到的網址，存檔。

## 三、正式上線 hosting（待決定）

目前 `index.html` / `manifest.json` / `sw.js` / `icon.jpg` 這組檔案可以放到任何靜態網頁空間，例如：
- GitHub Pages（免費）
- 公司既有網站的子路徑
- Netlify / Vercel（免費）

放上去後會有一個正式網址（例如 `https://your-domain.com/install-report/`），之後我可以根據這個網址幫你產生對應的 QR code 圖片，客戶掃碼後：
- Android／桌面瀏覽器：會提示「加入主畫面」，變成類似 APP 的圖示。
- iOS Safari：需要手動點「分享 → 加入主畫面」（iOS 對 PWA 沒有自動彈出安裝提示，需要在說明或現場稍微引導客戶一次）。

## 四、日後若要接現有 Fleet Dashboard

`裝機報告` Google Sheet 的欄位已經對齊現有 `裝機報告APP.xlsx` 的 19 欄結構，未來若要串接 `gen_dashboard.py`，只要把資料來源從讀 xlsx 改成讀這個 Google Sheet（可用 Apps Script 匯出 CSV，或用 Google Sheets API）即可，欄位對應不需要重新設計。

## 五、光針/耗材型號異動時的維護提醒

`index.html` 裡的 `TIPS` 陣列（前端顯示用）與 `AppsScript_Code.gs` 裡的 `TIPS` 陣列（後端欄位/通知信用）需要保持同步。日後若 LIGHTMED 光針型號有新增/下架，兩邊都要一起更新，否則新型號只會出現在前端表單、不會被記錄到 Google Sheet。
